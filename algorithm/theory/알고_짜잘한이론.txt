**
import sys
minV = sys.maxsize
print(minV)
9223372036854775807
이러면 정수중에 최고값 됨...
import sys 했을 때만 가능!!

보통 최대값 설정해놓을 때는
INF = 10000
이런식으로 해놓기~

**
a = [1, 2, 3, 4, 5]
print(*a)
1 2 3 4 5 출력됨!!

**
print(f'p={p} i={i}')
이런식으로 체크하면서 풀기!!